package com.marginallyclever.ro3.apps.viewport;

import com.jogamp.opengl.GL3;

/**
 * A placeholder class for future GL resources (shaders, buffers, etc).
 */
public interface OpenGL3Resource {
    void unload(GL3 gl3);
}
